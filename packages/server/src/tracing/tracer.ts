// Tracer — embedded in the agent loop, not bolted on via callback.
// run step = trace event. If a step has no trace, it's a code bug, not a missing registration.

export class Tracer {
  // TODO: implement
}
